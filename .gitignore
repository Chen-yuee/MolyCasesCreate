# ==============================================================================
# MolyCasesCreate 项目官方 .gitignore 配置
# 包含 Python 后端、Node 前端、数据文件以及个人环境配置的过滤规则
# （请根据你的实际需求，删除你【希望】提交到 Git 的那些行）
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. 核心运行时拦截（最致命导致 git pull 冲突与报错的文件）
# ------------------------------------------------------------------------------
# 后端产生的交互数据文件。你在网页上测试时，开发机会实时将数据写进这个文件。
# 如果不把它忽略掉，它在开发机上就会被标记为“待提交的代码变更”。下次你执行 git pull 拉取新代码时，就会因为本地存在待提交项而全部报错卡死！
web/backend/queries_store.json
# 用于存放 Deepseek API Key 之类的敏感私盐配置文件。一旦推上 Github 可能会导致 API 泄露被盗刷！
config.json

# ------------------------------------------------------------------------------
# 2. 前端环境及打包产物（极大拖慢 Git 速度的巨型文件夹）
# ------------------------------------------------------------------------------
# 存放前端几万个依赖代码的文件夹。绝对不要提交，应该让各自的机器自己 `npm install` 下载。
web/frontend/node_modules/
# 前端 Vite 打包后生成的生产环境静态网页文件夹，不需要用 Git 记录其变更。
web/frontend/dist/
# 旧版 npm 打包产物目录（如果有的话）
web/frontend/build/
# 前端依赖报错时的报错日志日志，毫无保留价值
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ------------------------------------------------------------------------------
# 3. 后端 Python 环境及缓存（极大拖慢 Git 速度的冗余数据）
# ------------------------------------------------------------------------------
# 存放 Python 虚拟环境的文件夹。同样不要提交，应该各自机器 `pip install -r requirements.txt`。
venv/
env/
.env/
# Python 运行文件时自动编译产生的二进制字节码缓存，无意义的二进制垃圾。
__pycache__/
*.py[cod]
*$py.class
# Python 日志、测试报告等
*.log
.coverage
htmlcov/

# ------------------------------------------------------------------------------
# 4. 业务数据集目录（视情况而定）
# ------------------------------------------------------------------------------
# 这里存放了原始的业务分析数据文件（如 locomo10_CN.json）。
# ⚠️ 注意：如果你希望你的同事一旦拉取代码就能自动获取所有的测试用例，那么 *请删掉下面这行* 以便让它保留在 Git 里！
# ⚠️ 但如果你的数据涉及公司机密，或者数据包极大（超过 100MB），请务必保留这行屏蔽它！
# data/

# ------------------------------------------------------------------------------
# 5. 电脑操作系统与 IDE 编辑器产生的玄学垃圾
# ------------------------------------------------------------------------------
# 苹果 macOS 操作系统下恶名昭著的文件夹位置记录缓存 
.DS_Store
.AppleDouble
.LSOverride
# 微软 Windows 操作系统下的缩略图缓存等
Thumbs.db
ehthumbs.db
Desktop.ini
# 你的当前代码编辑器（VSCode）生成的本地阅读偏好设置缓存
.vscode/
# 如果你或其他人用了 Pycharm/Webstorm 等 JetBrains IDE 产生的偏好缓存
.idea/
*.iml

# ------------------------------------------------------------------------------
# 6. 一些可能由于疏忽放入根目录的乱七八糟文件
# ------------------------------------------------------------------------------
# 将数据推导出为 csv、excel 或压缩包分析时忘删了导致的垃圾
# *.csv
*.xlsx
*.zip
*.tar.gz
